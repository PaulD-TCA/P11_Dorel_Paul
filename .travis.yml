language: python
python:
  - '3.6'

before_script:
  - pip3 install -r requirements.txt

env: DJANGO_SETTINGS_MODULE="purbeurre_plateforme.settings"

services:
  - postgresql

script:
  - python3 manage.py test

branches:
  only:
    - master
